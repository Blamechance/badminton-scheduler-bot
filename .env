# Environment Variables Template
# Copy this file to .env and fill in the values for local development
# On Replit, most of these are managed automatically via the Secrets/Environment tab

# Server Configuration
# Port for the Express server to listen on (default: 5000)
# Used in: server/app.ts
PORT=3000

# Node Environment
# Set to 'development' for local development, 'production' for deployment
# Used throughout the application for conditional logic
NODE_ENV=development

# Database Configuration
# PostgreSQL connection string for Drizzle ORM
# On Replit: This is automatically provided and managed by Replit's built-in database
# Format: postgresql://user:password@host:port/database
# Used in: drizzle.config.ts, server/app.ts (for database connection)
DATABASE_URL=postgresql://localhost:5432/badminton_scheduler

# Session Secret
# Secret key for encrypting session data (Express session middleware)
# Generate a random string for security (e.g., using: openssl rand -base64 32)
# Used in: server/app.ts (session configuration)
# On Replit: Managed as an encrypted secret via the Secrets tab
SESSION_SECRET=your-random-secret-key-here
